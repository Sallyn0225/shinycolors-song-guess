# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

《偶像大师 闪耀色彩》的中文圈粉丝，和他们的朋友。两种场景：

- **单人**：一个人戴耳机刷曲库，验证自己对 234 首歌的熟悉程度。随时开随时关，一局 10 或 20 题。
- **1v1**：两个人各自一台设备（同一局域网，或走公网实例），打一局歌牌式对战。
  两条配对路径：**公开房间**进大厅列表，谁都能点进去，用来跟没有带外沟通渠道的人配对；
  **私人房间**不进列表，把 6 位房间码发给朋友——这是面对面或语音开黑时更快的那条路。

设备优先级：**PC 与手机并重**，手机不能将就。局域网开黑基本都是掏手机；单人刷曲库更多在电脑前。
牌场（24 张牌、双方领地）在窄屏必须重新排布，不能只是把桌面版缩小。

## Product Purpose

听一段**去掉人声的伴奏**，认出它是哪首歌。

曲库是 234 首闪耀色彩 off vocal 音源，切成 1404 个 15 秒片段。两种玩法：

1. **单机**：四选一，简单 10 题 / 困难 20 题，按正确率与反应速度计分。
2. **1v1 空札領地戦**：日本竞技歌牌规则搬到听歌上——抢牌、送り札、お手つき，
   外加从曲库里抽出的**空札**（只会被播放、场上没有对应的牌，谁点谁お手つき）。先清空自陣者胜。

成功的样子：玩的人愿意连着打好几局，并且觉得输赢是自己耳朵和记忆力的结果，不是网络或界面的结果。

## Positioning

同类猜歌游戏猜的是**带人声的原曲片段**。这里猜的是**无人声伴奏**——难度来源从"记歌词"变成"记编曲"。
实测抽检 171 个切片：一听就认出 80%、想一下能认 19%、完全认不出 2%。

1v1 那套歌牌规则是别处没有的：空札机制让"不点"成为一种技术，
決まり字（听到前几个字就能锁定唯一一张牌）让记忆位置成为技术，送り札让取舍成为技术。

## Operating Context

- **必须戴耳机**，且蓝牙耳机有 150~300ms 输出延迟——判定按"相对片段起播的反应时间"而不是收包时间，
  所以延迟不影响胜负，但这件事必须让玩家看见并信任。
- **音频需要一次真实用户手势才能起播**。首页点难度、大厅点建房、刷新后点遮罩，都是这一下手势的载体。
- 1v1 全程有服务端权威时钟；双方 RTT **公开显示**——透明比假装公平更重要。
- 断线有 60 秒宽限，刷新后能接回牌面，但 AudioContext 会重新上锁，需要再来一次手势。

## Capabilities and Constraints

**已实现的完整界面清单（重写必须一个不少地覆盖）：**

| 界面 | 状态 |
|---|---|
| Start 首页 | 两个难度卡（题数/片段/限时/重听）、1v1 入口、错误条、耳机提示 |
| Play 单机答题 | 载入 / 答题 / 揭晓 / 出错四态；倒计时环 + 实时频谱；重听（余次）；四选一；答案与错选双标记；下一题 |
| Result 单机结算 | 总分与满分、评语、四项统计、逐题列表（封面、你选了什么、用时、重听次数、得分） |
| Lobby 1v1 大厅 | 昵称（记在本机）、创建房间（可命名 + 公开/私人，覆盖层）、公开房间列表（双行条目、等人/已满/对局中三态、断线降级）、6 位房间码加入、连接状态与 RTT、规则说明 |
| Room 房间内 | 房间名与可见性、房间码与复制、双方在线/RTT/准备状态、准备开局、被自动关闭的通知、离开房间回大厅 |
| Karuta 1v1 牌场 | memorize / waiting / live / choosing / reveal / over 六阶段；敵陣与自陣两个网格；決まり字加粗；点击交换位置；点牌抢答；送り札挑选与倒计时；回合叙述；对手掉线横幅；自己断线遮罩；刷新后手势遮罩；赛后统计表与再战投票 |
| App 壳 | 「正在找回对局」恢复态、座位丢失后的退场提示 |

**技术约束：**

- 栈固定：React 18 + Vite 6 + Tailwind v4 + TypeScript，pnpm workspace。
- **非 UI 代码不动**：`audio.ts`（Web Audio 单例、预取、精确调度、频谱、AAC 兜底）、
  `net/ws.ts`（WS 客户端、时钟同步、座位凭证）、`api.ts`、`features/*`（決まり字、稳定槽位、回合叙述）。
  这些文件里封着踩过的坑，重写会重新踩一遍。
- 倒计时**不能**用每帧 setState 驱动（React 18 并发调度会批处理，表现是"有时候没反应"）；
  必须保留 rAF 直写 DOM 的做法。
- 牌被取走后**位置要留空**，阵形不能重排——玩家背的就是位置。
- 领地**可能超过 12 张**（お手つき / 送り札 会送牌过来），牌场不能固定渲染 12 格。
- 8 个组合的官方代表色是既有事实数据（`unitColor`），界面里用它做曲目/牌的标识。

**术语（游戏内固定名词，不翻译）：** 空札 · 送り札 · お手つき · 自陣 / 敵陣 · 決まり字 · 勝ち / 負け / 引き分け

## Brand Commitments

- **文案语言：中文正文 + 日文术语。** 说明性文字用中文保证看得懂；游戏术语与大标题用日文保持气氛。
  这是确认过的约束，不要统一成单一语言。
- **视觉参照：** `design-extract-output/SHINYCOLORS-DESIGN-LANGUAGE.md`（人工校订版）是
  《アイドルマスター シャイニーカラーズ Song for Prism》官网设计语言的权威记录，
  用户已指定按它重建前端。同目录下工具自动生成的 token 文件含噪声（Cookie 弹窗的红色、Swiper 默认色、
  被误读的 px 尺度），该文档 §0 逐条列明，不得采用。
- 这是粉丝向的非商业项目，界面里不出现官方商标之外的品牌主张。

## Evidence on Hand

- `design-extract-output/screenshots/`：官网 full-page / hero / nav / card / button 实拍截图。
- `design-extract-output/_raw/share.css`、`lib.css`：官网原始 CSS，设计语言文档全部结论的依据。
- `assets/cover`、`assets/thumb`：234 首曲目的真实封面与缩略图（构建产物）。
- 8 个组合的官方代表色 hex，来源 アイマスDB，已核验。
- **没有**官网的背景位图（`bg_back_pc.png` 虹彩镭射膜、`bg_geo_pc.svg` 晶体碎片）。
  已确认用纯 CSS/SVG 程序化重建，**不要**声称拿到了官方素材。
- **没有**真实玩家数据、评价、排行榜。切片抽检的 80/19/2 分布是内部实测，不是用户调研。

## Product Principles

1. **听觉优先，视觉让路。** 玩的时候屏幕上唯一该看的是"还剩多久"和"现在在播吗"。
   界面再好看，也不能在播放期间抢注意力。
2. **判定要可见地公平。** RTT、被校正次数、自动送札的剩余秒数——凡是影响胜负的机制都摆在明面上，
   靠透明而不是靠承诺建立信任。
3. **位置即记忆。** 牌场的空间关系是玩法本身，任何会让阵形重排、位置漂移的视觉决定都是 bug。
4. **每一次卡住都要有出路。** 断线、座位丢失、音频没解锁、对手掉线——每种失败态都必须
   告诉玩家发生了什么、还要等多久、能做什么。
5. **不给作弊者送线索。** 界面不显示曲目时长、切片编号、加载差异等任何可用于建立对照表的信息。

## Accessibility & Inclusion

**硬性要求，重写后必须逐条保住：**

- 键盘可完成单机全流程：`1`–`4` 选项、`R` 重听、`Enter` / `Space` 下一题。
- `prefers-reduced-motion: reduce` 下关闭入场、抖动、光晕、跑马灯等动画。
- 可见的 `:focus-visible` 焦点环。
- 所有图标化/纯色状态标记带 `aria-label`；错误信息带 `role="alert"`。
- 正文与状态色的对比度必须达标。**官网那套白底淡紫字有对比度风险**——
  重建时正文色要压深，不能照抄 `#615f90` 当正文色。
