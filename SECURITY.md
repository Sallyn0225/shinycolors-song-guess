# 安全策略

## 支持版本

只支持 `main` 分支的最新提交。本项目没有维护稳定版分支。

## 如何报告漏洞

**请走 GitHub 的私密漏洞报告，不要开公开的 issue。**

在这个仓库页面：**Security → Report a vulnerability**（即 GitHub 私密漏洞报告 / Security Advisories 通道）。你的报告只有维护者可见，确认前不会公开。

报告时尽量附上：

- 受影响的组件（`apps/server` 的 HTTP / WebSocket、房间配额、还是 `apps/web` 客户端）
- 复现步骤或 PoC
- 你对影响面的判断（能做什么、做不到什么）

## 范围与已知边界

欢迎报告的问题类型：

- 公网部署形态下的服务端漏洞：配额 / 限流绕过、房间码暴力枚举、WS 消息注入、资源耗尽、敏感信息泄漏
- Web 客户端漏洞：XSS、第三方依赖问题
- 部署链路问题：镜像 / 配置里的不安全默认值

**这些不算漏洞**，是明示的设计边界，开 issue 讨论即可：

- **音频能被决心作弊的人保存下来** —— 音频必须送达客户端才能播放，这是原理上限。项目的防作弊定位是「挡住打开 DevTools 的休闲作弊」，不是密码学安全（README「核心设计约束」有说明）。
- **联机判定可被恶意少报 RTT 获得小幅度优势** —— 这是已知上限，压制手段已在实现里，改进建议走普通 issue。
- **没有任何人工审核的滥用治理**（举报 / 封禁）—— 目前只有机器可判定的速率与配额限制，缺这一环是产品现状，不是疏漏。

## 响应

个人维护的开源项目，没有 SLA，但会在合理时间内响应。修复会以普通提交合入 `main`，公开披露的时机由报告者和维护者商定。
